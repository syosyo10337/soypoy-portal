#!/bin/bash
set -euo pipefail

# Docker Compose開発環境を起動

echo ""
echo "🚀 開発環境を起動しています..."
echo "🔹 Neon Local: エフェメラルブランチを自動作成"
echo "🔹 Next.js: http://localhost:3000"
echo ""

# dbサービス用の.env.dockerを生成（dotenvxのログを除外）
./bin/runenv sh -c 'env | grep -E "^(NEON_|PARENT_BRANCH_ID=)"' 2>&1 | grep -v "dotenvx" > .env.docker

exec ./bin/runenv docker compose up
